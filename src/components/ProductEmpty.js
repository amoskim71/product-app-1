import React, { Component } from 'react';

class ProductEmpty extends Component {
    render() {
        return (
            <div className="product-empty">Nothing found.</div>
        );
    }
}

export default ProductEmpty;